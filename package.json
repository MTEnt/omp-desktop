{
  "name": "omp-desktop",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "ui:dev": "npm --prefix ui run dev",
    "ui:build": "npm --prefix ui run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
